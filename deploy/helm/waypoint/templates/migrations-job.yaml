## Migration Job — pre-upgrade only (NOT pre-install). On first install, postgres
## doesn't exist yet, so the Job would fail. Startup migration in Program.cs handles
## install; this Job handles ongoing upgrades.
{{- if not .Release.IsInstall }}
apiVersion: batch/v1
kind: Job
metadata:
  name: waypoint-migrate-{{ .Release.Revision }}
  namespace: {{ .Values.namespace }}
  annotations:
    "helm.sh/hook": pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: "{{ .Values.api.image.repository }}:{{ .Values.api.image.tag }}"
          command: ["dotnet", "Waypoint.Api.dll", "migrate"]
          env:
            - name: ConnectionStrings__Postgres
              valueFrom:
                secretKeyRef: { name: waypoint-config, key: postgres-connection-string }
{{- end }}
